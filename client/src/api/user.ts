const findSession = async () => {
  try {
    console.log('find-session');
  } catch (error) {
    console.log(error);
  }
};

const login = async () => {
  try {
    console.log('login');
  } catch (error) {
    console.log(error);
  }
};

const logout = async () => {
  try {
    console.log('logout');
  } catch (error) {
    console.log(error);
  }
};

const register = async () => {
  try {
    console.log('register');
  } catch (error) {
    console.log(error);
  }
};

export default {
  findSession,
  login,
  logout,
  register,
};
